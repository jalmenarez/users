apply plugin: "war"

bootWar {
    mainClassName = "cl.jalmenarez.users.UsersApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
